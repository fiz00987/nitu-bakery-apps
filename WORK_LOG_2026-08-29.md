# 📋 Work Log — August 29, 2026

**Commit pushed to GitHub:** `bfe59ed` on `main` → https://github.com/fiz00987/nitu-bakery-apps.git
**Files changed:** `admin-app/index.html`, `admin-app/utils.js`, `customer-app/index.html` (607 insertions, 127 deletions)

---

## 1. Rocket payment removed everywhere — `full` is admin-only
- Removed the Rocket option and all `ROCKET_RATE` / `case 'rocket'` branches from the admin app's `calculatePayment()` and `approveCalculation()`.
- Verified: **zero rocket references** remain in the entire repo.
- Customer app never had Rocket. The `✅ ফুল পেমেন্ট` option now exists **only** in the admin app.

## 2. Customer photo note visible in admin
- New **📝 ছবির নোট** editable field in the admin edit modal.
- On the order card it renders as a highlighted amber call-out box (`📝 ছবির নোট: …`).

## 3. Admin time input = customer app style
- Replaced free-text time with **hour input + AM/PM selector** (e.g. `3.00` + `PM`).
- Same rules as customer app: auto `.00` minutes (`3` → `3.00`), Bengali digits accepted, hour 1–12 / minutes 0–59.
- Legacy times (e.g. "বিকাল ৪টা") preserved and re-open correctly when editing old orders.
- Saves as `timeSlot` / `timeSlotLabel` — identical to customer orders.

## 4. Manual admin orders now identical to customer orders
`saveOrder()` writes the same field set as the customer app:
`orderId` (`NByyyymmdd####` format), `customerName`, `customerPhone`, `category`/`categoryName`, `weightLabel`, `deliveryDate`, `deliveryAddress`, `cakeWriting`, `basePrice`/`cakePrice`/`weightPrice`/`subtotal`, `fulfilment` (new 🚚 ডেলিভারি ধরন dropdown — pickup auto-fills address & zeroes delivery charge), `paymentMethod`/`paymentMethodName`, `trx` (new), `notes` (new), `photoNote`, `timeSlot`/`timeSlotLabel`, `createdAt` preserved.
→ Manual orders look identical to online orders in every view, including customer app "Previous Orders".

## 5. Charge calculation = customer app exactly
- Formula: `charge = Math.ceil(base × rate)` — same ceiling rounding (bKash 2800 → ৳51, pays ৳2,851).
- Auto-calc feeds `f-paid` + `f-charge-deduct`; `saveOrder` syncs `bkashCharge` = `paymentCharges` = charge; `advance`/`advanceTotal`/`dueAmount` derived exactly like the customer app.

## 6. Pending + manual confirm (recommended & implemented)
- **⏳ পেন্ডিং** status option added; it is the **default for every new manual order**.
- Customer orders already arrive as `pending`.
- Pending orders show normally in Plan/Orders tabs with a ⏳ chip; confirm with one tap from the card's status dropdown.
- Customer app **displays nothing about status** (see #8b) but the DB field `status: 'pending'` stays so the admin workflow works.

## 7. Push notifications when app closed — diagnosis
Code pipeline (`cloud-functions/index.js` → `/pushTokens` → `firebase-messaging-sw.js`) is **correct**; failures are deployment/config:
1. **Deploy cloud functions** (needs Blaze plan): from `cloud-functions/` run `npm install firebase-functions firebase-admin` then `firebase deploy --only functions`. Confirm **2 functions** exist (`onOrderCreate`, `dailyOrderSummary`).
2. **Register tokens:** open admin app, tap 🔔 once per device, then verify entries in Realtime Database → `/pushTokens` (with `platform`).
3. **iOS:** must be installed via Safari → Share → **Add to Home Screen** and opened from the icon (normal Safari tabs cannot receive push).
4. **Android:** a WebView APK **cannot** receive push when closed — use Chrome → ⋮ → *Add to Home Screen* instead.
5. Test: force-close app → place an order; if nothing arrives check `/pushTokens` then Functions logs in Firebase console.

## 8a. Daily "orders placed today" popup (ADMIN APP ONLY)
- On admin app open, after data loads, shows **🎉 আজকের নতুন অর্ডার (Nটি)** popup, numbered list:
  *1. Firoz Ahmed — অর্ডার করেছে 2 pound ব্ল্যাক ফরেস্ট কেক — মূল্য: ৳2,800 · ডেলিভারির তারিখ: 21 September Monday 2026*
- List **accumulates all day** (orders since midnight Bangladesh time, `bdTodayStr()` = UTC+6), **resets automatically at 12 AM**.
- Cancelled orders excluded. Shows once per session (`dailyPopupShownThisSession`).
- **🆕 আজকের নতুন [count]** button in top bar reopens it anytime, with live count badge.
- Implementation locations in `admin-app/index.html`: overlay HTML ~line 1814, 🆕 button ~line 1888, auto-show ~lines 2675–2677, logic ~lines 4442–4502.
- **Confirmed: zero references in customer app — admin only.**

## 8b. Status/pending hidden from CUSTOMERS (customer-app/index.html)
- **Track-order result:** no longer shows status; title changed "অর্ডার স্ট্যাটাস" → "আপনার অর্ডার"; shows only order ID · মোট ৳ · ডেলিভারি date.
- **Order history modal:** removed the "Status / অবস্থা" row — customer sees Order ID, Cake, Writing, Address, Total, Paid, Due only.
- Removed unused `previousOrderStatus()` function and `.order-history-status` CSS.
- **Kept** `status: 'pending'` in the DB write (invisible to customers, required by admin workflow).
- Customers now never see পেন্ডিং / অবস্থা / any status value.

## Bonus fix
- `admin-app/utils.js`: stale 10-flavour `FLAVOURS` array synced to customer app's **15 flavours**.

---

## ✅ Verification summary
- All 3 admin inline script blocks + customer 2 script blocks pass Node syntax checks; `utils.js` parses clean.
- Logic tests passed: BD-midnight reset window, charge formula (2800→51; 50% advance of 2800→26), time normalize (`3`→`3.00`→`3:00 PM`), legacy time round-trip.
- Repo grep: 0 rocket refs; daily popup refs only in admin app (25).

## 🔔 Reminders / not pushed
- **Deploy** cloud functions + hosting to make changes live (git push alone doesn't update the apps).
- Untracked files **not committed** (intentional): `customer-app/medium cake.txt`, `customer-app/mini cake.txt`.
